VITE_API_BASE_URL=/api
VITE_TELLER_USERNAME=teller
# VITE_TELLER_PASSWORD is intentionally absent.
# Passwords must never be stored in .env — they would be inlined into the JS bundle
# by Vite and readable by anyone who downloads the app. Tellers type the password manually.
